# Graphify Configuration
# This file is committed to git and shared with the team
# For secrets (API keys), see .env file

[input]
source = "."
type = "auto"

[output]
directory = "graphify-out"
format = ["json", "html", "yaml", "xml"]

[model]
name = "mistralai/mistral-7b-instruct:free"

